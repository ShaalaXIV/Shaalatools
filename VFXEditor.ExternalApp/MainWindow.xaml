<Window
    x:Class="VFXEditor.ExternalApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:VFXEditor.ExternalApp.ViewModels"
    mc:Ignorable="d"
    Title="VFX Editor External">

    <Window.DataContext>
        <viewModels:MainViewModel />
    </Window.DataContext>

    <Grid Padding="16" ColumnSpacing="12" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="24" FontWeight="SemiBold" Text="VFX Editor External (Prototype)" />

        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Header="Penumbra Mods Folder"
                     PlaceholderText="C:\\XIVLauncher\\pluginConfigs\\Penumbra\\mod"
                     Text="{Binding ModsDirectory, Mode=TwoWay}" />
            <Button Grid.Column="1" Content="Scan" Command="{Binding ScanCommand}" />
            <Button Grid.Column="2" Content="Save" Command="{Binding SaveCommand}" />
        </Grid>

        <Grid Grid.Row="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="Discovered Editable Assets" />
                <TextBlock Margin="0,0,0,8" Opacity="0.7" Text="Includes .pap, .avfx, .tmb, .eid, .uld, .atex, .tex, .atch, .sklb, .skp, .shpk, .shcd, .mtrl, .mdl (no .scd)." TextWrapping="WrapWholeWords"/>
                <ListView Height="560"
                          ItemsSource="{Binding Entries}"
                          SelectedItem="{Binding SelectedEntry, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,4">
                                <TextBlock FontWeight="SemiBold" Text="{Binding RelativePath}" />
                                <TextBlock Opacity="0.7" Text="{Binding ModName}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="Simple Raw Editor" />
                <TextBlock Margin="0,0,0,8" Opacity="0.7" Text="Save automatically creates timestamped backups before overwriting." />
                <TextBox AcceptsReturn="True"
                         Height="560"
                         TextWrapping="NoWrap"
                         FontFamily="Consolas"
                         Text="{Binding EditorContent, Mode=TwoWay}" />
                <TextBlock Margin="0,8,0,0" Text="{Binding StatusText}" TextWrapping="WrapWholeWords" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
